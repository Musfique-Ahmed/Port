# Example environment file. Copy to .env.local and fill in real values.
# (The portfolio currently does not read any environment variables at runtime;
# all data lives in /lib/site.ts and /lib/projects.ts.)
NEXT_PUBLIC_SITE_URL=https://musfique-ahmed.github.io/portfolio/
